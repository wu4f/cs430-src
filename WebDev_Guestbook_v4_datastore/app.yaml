runtime: python37
env: standard

# Can run via flex environment, but it is a bit more expensive
# and is not covered by the free tier
#
#runtime: python
#env: flex

entrypoint: gunicorn -b :$PORT app:app

runtime_config:
  python_version: 3

manual_scaling:
  instances: 1

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
